/* eslint-disable react/prop-types */

const AlertDanger = ({ message }) => {
  return <p className="w-full text-sm font-semibold text-red-600 m-2 pl-2">{message}</p>;
};

export default AlertDanger;
